public void plusOne(List<Integer> numbers) {
    // code here
    Integer arraySize = numbers.size(); //4
    Integer lastDigit = numbers.get(arraySize - 1);
    if(lastDigit < 9){
        numbers.add(arraySize-1, lastDigit+1);
        numbers.remove(arraySize-1);
    }
}