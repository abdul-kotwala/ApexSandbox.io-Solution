public void plusOne(List<Integer> numbers) {
    // code here
    Integer arraySize = numbers.size();
    Integer lastDigit = numbers.get(arraySize);
    if(lastDigit < 9){
        numbers.add(arraySize, lastDigit+1);
    }
}