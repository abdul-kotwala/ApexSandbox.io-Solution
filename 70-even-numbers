public List<Integer> evenNumbers(Integer n) {
    List<Integer> evenNumbersList = new List<Integer>():
    Integer loopVar = 10;
    for(Integer i = 1; i <= loopVar; i++){
        if(Math.mod(i,2)==0){
            evenNumbersList.add(i);
        }
    }

    return evenNumbersList;
}