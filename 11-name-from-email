public String nameFromEmail(String email) {
    //code here
    
    String fullName = email.substringBefore('.').toLowerCase().capitalize() + ' ' + email.substringAfter('.').substringBefore('@').toLowerCase().capitalize();

    return fullName;
}